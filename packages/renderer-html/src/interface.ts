/**
 * STRA Renderer Interface - Re-exported from @stra/types.
 *
 * Renderer is just a projection, not the source of truth.
 * Interface definitions live in @stra/types so all renderer packages
 * share the same contract.
 */

export type { Renderer, RendererProjection, RenderOutput, ProjectionDiff } from '@stra/types';
